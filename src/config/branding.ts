/**
 * Internal app name. Single source of truth — never hardcode this string anywhere else.
 * Renaming the app means changing only this constant.
 */
export const APP_NAME = "Scrabble Babble" as const;
